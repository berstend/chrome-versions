{
  "name": "chrome-versions",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "ts": "node -r esbuild-register",
    "check": "tsc --noEmit --incremental false --composite false --tsBuildInfoFile null",
    "update-versions": "yarn ts src/index.ts"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.6.1",
    "esbuild": "^0.14.39",
    "esbuild-register": "^3.3.2",
    "typescript": "^4.6.4"
  },
  "dependencies": {
    "node-fetch": "2"
  }
}
